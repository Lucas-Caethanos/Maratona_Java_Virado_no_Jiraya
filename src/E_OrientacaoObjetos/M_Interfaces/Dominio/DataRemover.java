package E_OrientacaoObjetos.M_Interfaces.Dominio;

public interface DataRemover {
    // Método_ abstrato que precisa ser implementado pelas classes que implementam esta interface.
    // Por padrão, os métodos de interface são públicos e abstratos.
    void remove();

}
